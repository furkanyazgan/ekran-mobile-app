import 'package:flutter/material.dart';

class LocationNavigator extends StatelessWidget {
  const LocationNavigator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Location Page"),);
  }
}
